{
  "name": "tysiac-game",
  "version": "1.0.0",
  "description": "Online Tysiąc Card Game",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "start:backend": "cd backend && npm run dev",
    "start:frontend": "cd frontend && npm start",
    "start:all": "cd backend && npm install && cd ../frontend && npm install && concurrently \"cd backend && npm run dev\" \"cd frontend && BROWSER=none npm start\""
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
